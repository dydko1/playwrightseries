package trash123.stream.exc5;

import org.testng.annotations.Test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.concurrent.atomic.AtomicInteger;
import java.util.function.Predicate;
import java.util.function.Supplier;
import java.util.regex.Pattern;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class Test051 {

    Stream<String> nameString = Stream.of("John", "Marry", "George", "Paul", "Alice", "Ann");

    @Test
    public void nameStream() {
        nameString
                .filter(s -> s.contains("A"))
                .map(String::toUpperCase)
                .forEach(System.out::println);
    }

    @Test
    public void sortedName() {
        nameString.filter(s -> s.contains("A"))
                .map(String::toUpperCase)
                .sorted()
                .forEach(System.out::println);
    }

    @Test
    public void intermediateName() {
        nameString.filter(s -> {
            System.out.println("filter: " + s);
            return true;
        }).forEach(System.out::println);
    }

    @Test
    public void map122() {
        List<String> strings = Arrays.asList("a1", "a2", "b3", "b4", "c5", "c6");

        strings
                .stream()
                .map(s -> s.substring(1))
                .mapToInt(Integer::parseInt)
                .average()
                .ifPresent(System.out::println);
    }

    @Test
    public void illegalState() {

        Stream<String> namesStream =
                Stream.of("John", "Marry", "George", "Paul", "Alice", "Ann");
        Predicate<String> funHas = s -> s.equals("Alice");

        namesStream
                .anyMatch(funHas);  // ok
        namesStream
                .noneMatch(funHas); // nok
    }

    @Test
    public void get1() {
        Supplier<Stream<String>> namesStreamSupplier =
                () -> Stream.of("John", "Marry", "George", "Paul", "Alice", "Ann");

        Predicate<String> hasName = s -> s.equals("Alice");

        namesStreamSupplier.get().anyMatch(hasName);
        namesStreamSupplier.get().noneMatch(hasName);
    }

    @Test
    public void col2(){
        final int chunkSize = 3;
        final List<Integer> integers =
                Arrays.asList(2, 4, 6, 8, 10, 12, 14, 16);

        AtomicInteger counter = new AtomicInteger(0);

        Stream<List<Integer>> integerListStream =
                integers
                        .stream()
                        .collect(Collectors.groupingBy(integer -> counter.getAndIncrement()/chunkSize))
                        .entrySet()
                        .stream()
                        .map(Map.Entry::getValue);

        List<List<Integer>> chunkIntegersList =
                integerListStream
                        .collect(Collectors.toList());
        System.out.println("----------------------");
        System.out.println(chunkIntegersList);
    }

    @Test
    public void flat2(){
        
    }
}